nodetype tempRight= pq.top(); // 더 작은값이 top 에 있다.
      // answer.push(temp1);
      // pq.pop();
      // nodetype tempLeft = pq.top(); //더 큰 값이 top의 바로 뒤에 있다.
      // answer.push(temp1);
      // pq.pop();
      // temp.symbol = "";
      // temp.frequency = tempRight.frequency + tempLeft.frequency;
      // temp.left = *tempLeft;
      // temp.right = *tempRight;
      // answer.push(temp);